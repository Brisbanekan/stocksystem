# 年節禮盒庫存系統 — 規劃文件

小團隊自用（1–10 人）｜Google Sheets + Apps Script 架構｜支援 LINE 文字指令進出料｜含年節送禮規劃

---

## 1. 系統概觀

這是一套零伺服器、零月費的年節禮盒庫存管理系統，專為「採購禮盒、登記庫存、規劃送禮、出貨對外」這條流程設計。資料存在 Google Sheets，後端與 LINE Bot 全部跑在 Google Apps Script 上，前端是一個網頁儀表板。倉管人員可以在電腦上用網頁操作，現場人員則用 LINE 打一行字就能登記進出料，兩邊共用同一份即時庫存。網頁採響應式設計（RWD），電腦、平板、手機皆可使用：手機上頁首導覽列可橫向捲動、KPI 卡片自動收成兩欄、各表格可左右滑動檢視，方便現場直接用手機開網頁操作。

每個禮盒品項都帶有分類、規格、年分與節日；進料、出料兩種異動會即時更新庫存並寫入紀錄；即時庫存量隨每筆異動自動計算並在儀表板與 LINE 查詢顯示；網頁端額外提供年節統整、出貨明細與送禮規劃三個檢視，讓使用者能依年分／節日彙總、追蹤每個出貨對象，並事先規劃每位對象要送哪些禮盒。

## 2. 為什麼選這個架構

對 1–10 人的團隊來說，這個組合的優點是免費、免維護、上手快。Google Sheets 本身就是大家熟悉的試算表，隨時可以手動開來看或修正資料、做樞紐分析、匯出報表；Apps Script 內建在 Google 帳號裡，部署成網頁應用程式後就能同時當前端網站與 LINE Webhook，不需要租主機或資料庫。代價是它不適合高併發或巨量資料（數萬筆以上異動會開始變慢），但以小團隊一年幾次年節採購的量來說綽綽有餘，未來若成長再換正式資料庫即可。

## 3. 資料表設計（Google Sheets 分頁）

系統用一份試算表、兩個分頁。

**Items 分頁（禮盒主檔，即時庫存）**

| 欄位 | 說明 | 範例 |
|---|---|---|
| ID | 唯一識別碼（系統自動產生，格式 G+時間戳） | G1 |
| 分類 | A=公益、B=聖保羅、C=鬥茶王 | A |
| 禮盒名稱 | 顯示名稱，LINE 可用此模糊查詢 | 公益月餅禮盒 |
| 規格 | 規格描述 | 8入/盒 |
| 年分 | 適用年分 | 2026 |
| 節日 | 過年 / 端午 / 中秋 | 中秋 |
| 單位 | 計量單位 | 盒 |
| 即時庫存 | 系統自動維護，每筆異動更新 | 120 |

**Log 分頁（異動紀錄，稽核軌跡）**

| 欄位 | 說明 |
|---|---|
| 時間 | 自動帶入 |
| 類型 | 進料 / 出料 / 刪除 |
| 禮盒名稱 | 異動品項 |
| 年分、節日 | 異動品項的年分與節日 |
| 數量 | 本次異動量 |
| 結存 | 異動後的庫存量（凍結當下數字，方便日後對帳；刪除類型記為「—」） |
| 人員/備註 | 進料填供應商、出料填出貨對象、刪除填原因 |
| 來源 | web 或 line，區分操作管道 |

「即時庫存」直接存在 Items 分頁而非每次加總 Log，是為了讓查詢即時又快；Log 則保留完整歷史供稽核。兩者由程式在每筆異動時同步更新，並用 Apps Script 的 LockService 鎖定避免多人同時操作時數字錯亂。出貨明細（出貨對象、各對象送了多少）即是從 Log 中所有「出料」紀錄推導而來，不另存一份。

## 4. 功能流程

**進料**：新採購禮盒入庫。輸入禮盒與數量，庫存增加，寫入紀錄。備註欄可填供應商或採購單號。

**出料**：對外送禮或出貨。庫存減少，會先檢查庫存是否足夠，不足則擋下並回報剩餘量。備註填出貨對象（致贈廠商、客戶、村長等），這也是出貨明細的彙整依據。

**刪除禮盒**：移除不再使用的禮盒品項。刪除前會把當下庫存與原因寫入 Log（類型記為「刪除」）留底，再從 Items 移除該列，確保稽核軌跡不中斷。

**新增禮盒**：建立新品項，需指定分類（A/B/C）、名稱、規格、年分、節日、單位與期初庫存。若期初庫存大於 0，會自動補一筆「進料／期初」紀錄。

**送禮規劃頁（系統首頁）**：在實際出貨前，先規劃每個年節要送誰、送什麼。可切換公司（多公司）、選擇年分與節日，以部門分組列出送禮對象，每位對象可填預計總數與三類禮盒（公益／聖保羅／鬥茶王）的分配數量、備註、簽收人與狀態（待備貨／已備貨／已送達）。當「已分配（三類加總）」不等於「預計總數」時，該列會亮起橘色提醒尚未決定送哪種禮盒。按「送達」會依各類別逐筆執行出料、扣減實際庫存並寫入出貨明細。已送達的列可隨時復原：把狀態改回「待備貨／已備貨」時，系統會自動把扣掉的庫存加回、移除對應的出貨紀錄，並解除鎖定讓該列重新編輯（送達期間該列欄位暫時鎖定，以維持庫存與紀錄一致）。此頁為開啟網頁時的預設首頁。

**即時庫存量（庫存管理頁）**：儀表板頂部顯示禮盒品項數、總庫存（盒）、今日異動筆數；下方表格列出每個禮盒的分類、名稱、規格、年分、節日、單位與即時庫存，可依分類篩選、依名稱／規格搜尋；另有異動紀錄區顯示近期進出料。

禮盒庫存清單採**自動排序**，不需手動調整：以「目前要準備的年節」為優先排最上面，其後的未來年節依日曆先後（過年 → 端午 → 中秋）往下排；已備妥／已過的年節排在清單後段（較近期者在前）。考量送禮會提前準備，系統採「某年節將至（進入它發生的月份）即視為已備妥，焦點自動移到下一個要準備的年節」的判斷（農曆逐年浮動，以月份近似：1 月→今年過年、2–5 月→端午、6–8 月→中秋、9–12 月→隔年過年）。例如 6 月端午將至，最上面會自動換成接下來要張羅的中秋。同一年分同一節日內再依禮盒名稱 a→z（如聖保羅a、聖保羅b、聖保羅c）；新增禮盒會自動插入正確位置，排序與篩選、搜尋並存。

**年節統整頁**：以年分為主軸彙總，顯示涵蓋年分數、目前總庫存、累計進料與累計出料，並按年分分組列出各禮盒，方便回顧每一年各節日的採購與消耗概況。

**出貨明細頁**：彙整所有「出料」紀錄，顯示累計出貨數、出貨筆數、出貨對象數，提供各出貨對象的彙整與逐筆明細，並可依出貨對象／禮盒搜尋，掌握「誰收了哪些禮盒、各幾盒